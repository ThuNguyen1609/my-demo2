const x = 19;
let compareWith0 = "";

if (x > 0) {
    compareWith0 = "Giá trị bạn nhập là số dương";
} else if (x < 0) {
    compareWith0 = "Giá trị bạn nhập là số âm";
} else {
    compareWith0 = "0 không phải số nguyên dương cũng không phải số nguyên âm bạn nhé";
}
console.log (compareWith0);
